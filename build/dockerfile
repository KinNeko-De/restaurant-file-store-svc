FROM debian:bookworm-slim AS base

RUN apt-get update && apt-get upgrade -y

EXPOSE 8080

ARG USER_UID=1001
RUN useradd -u ${USER_UID} appuser

WORKDIR /app

COPY ./bin/app .

USER ${USER_UID}
CMD ["/app/app"]